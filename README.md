# self-supervised-collider-foundation-model
Developed a Transformer-based foundation model prototype for collider event representation learning. The model performs self-supervised pretraining on simulated particle collision events by masking particle tokens and reconstructing continuous kinematic features and particle identities. Implemented in PyTorch and trained on CERN SWAN GPU.
